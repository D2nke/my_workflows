name: Deploy (Fly.io)

on:
  workflow_call:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
  
    - name: "Deploy ${{ inputs.fly_app }} → ${{ inputs.environment }}"
      uses: D2nke/actions/deploy@feat/new_code_base